import { Typography } from '@material-ui/core'
import React, { FC } from 'react'

export const PlayList: FC = () => {
    return (
        <div>
            <Typography variant="h5" component="h2">Плейлист</Typography>
        </div>
    )
}
