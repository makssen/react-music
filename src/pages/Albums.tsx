import React, { FC } from 'react';
import { Typography } from '@material-ui/core'

export const Albums: FC = () => {
    return (
        <div>
            <Typography variant="h5" component="h2">Альбомы</Typography>
        </div>
    )
}
